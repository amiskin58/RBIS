function TopScoreBoard() {
  return (
    <div
      style={{
        background: "#1f2937",
        color: "white",
        borderRadius: "10px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      Top ScoreBoard
    </div>
  );
}

export default TopScoreBoard;